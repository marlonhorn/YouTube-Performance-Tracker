import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.marlonhorn.youtubeperformancetracker',
  appName: 'YouTube Performance Tracker',
  webDir: 'www'
};

export default config;
